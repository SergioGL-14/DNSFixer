name: Tests

on:
  push:
    branches: [main]
  pull_request:

jobs:
  security-tests:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Run security tests
        shell: pwsh
        run: ./tests/Test-DNSFixerSecurity.ps1
